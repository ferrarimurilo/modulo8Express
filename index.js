const express = require("express");

const app = express();

const porta = 3000;

app.listen(porta, () => {
    console.log(`O servidor está ativo pela porta ${porta}`);
    console.log(`O servidor está ativo pela porta ${porta}`);

});


